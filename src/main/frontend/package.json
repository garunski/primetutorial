{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf ../webapp/assets",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
